# 代码功能介绍

本文档说明 Power Insight Grid 开源整理版的代码结构、主要模块和功能边界。

## 1. 总体架构

项目由三个主要部分组成：

```text
Power Insight Grid
├─ frontend              # Next.js 前端页面
├─ backend               # FastAPI 后端服务
├─ gd-market-crawler     # 数据获取控制服务
├─ deploy                # 部署示例
└─ docs                  # 项目说明文档
```

整体数据流如下：

```text
Excel / 演示数据 / 爬虫下载文件
        ↓
backend 导入与解析
        ↓
SQLite 本地数据库
        ↓
FastAPI 接口
        ↓
Next.js 前端页面
```

## 2. 前端代码

前端目录：

```text
frontend
```

主要职责：

- 提供辅助决策系统的页面入口。
- 展示基本面数据、市场出清数据、网架拓扑、政策文件和导入管理。
- 通过 API 读取后端数据库中的结构化结果。
- 提供日期选择、对比展示、图表联动和状态提示。

核心技术：

- Next.js
- React
- TypeScript
- CSS Modules 或普通 CSS

典型页面：

- 首页：系统导航和交易日状态。
- 现货模块：基本面、市场出清与分时电量、节点电价入口。
- 网架拓扑：线路价差、阻塞识别和节点关系展示。
- 政策文件：政策文本管理与结构化解读入口。
- 数据获取：爬虫任务、缺失检查、入库状态和同步管理。
- 导入管理：样例数据和外部文件导入。

## 3. 后端代码

后端目录：

```text
backend
```

主要职责：

- 提供 FastAPI 服务。
- 管理本地 SQLite 数据库。
- 解析 Excel / CSV 数据文件。
- 为前端提供统一查询接口。
- 提供数据导入、数据质量检查和状态汇总。

常见接口能力：

- 交易日上下文查询。
- 基本面数据查询。
- 市场出清和分时电量查询。
- 节点电价和全省均价查询。
- 网架拓扑节点、线路和阻塞识别结果查询。
- 数据导入和同步状态查询。

## 4. 数据导入与数据库

开源版默认不提交真实数据库文件。数据库通常在本地运行时生成。

建议数据流程：

1. 准备脱敏后的样例 Excel / CSV 文件。
2. 通过导入接口或导入脚本写入 SQLite。
3. 前端页面通过后端接口读取数据库。
4. 页面按交易日和数据类型展示。

开源版应避免提交以下内容：

```text
*.db
*.sqlite
data_samples/
uploads/
outputs/
gd-market-crawler/downloads/
gd-market-crawler/browser-data/
```

## 5. 网架拓扑模块

网架拓扑模块用于展示线路、节点和价格分化关系。

主要能力：

- 读取网架线路和节点关系。
- 根据节点价格计算线路两端价差。
- 识别超过阈值的疑似阻塞线路。
- 支持分时点查看。
- 支持拓扑图缩放、线路悬停和节点信息查看。

开源版保留的是通用展示框架和演示逻辑，不包含私有网架文件或真实运行数据。

## 6. 政策文件模块

政策文件模块用于管理电力市场政策、规则、通知和实施细则。

主要能力：

- 上传和管理政策文件。
- 展示政策摘要和结构化字段。
- 支持后续接入本地或外部模型进行政策解读。

开源版不包含真实内部政策解读结果和私有提示词配置。

## 7. 数据获取工具

数据获取目录：

```text
gd-market-crawler
```

主要能力：

- 管理市场数据下载任务。
- 支持按日期范围检查缺失数据。
- 支持补充获取和覆盖获取。
- 支持任务中断和运行日志。
- 支持爬取后校验文件，再同步进入数据库。

开源版不包含真实登录会话、Cookie、UKey 信息或本地浏览器缓存。

## 8. 部署示例

部署目录：

```text
deploy
```

可用于放置示例启动脚本、反向代理配置或容器化配置。

开源版建议只保留通用示例，不提交本地机器路径、私有域名、内网地址和生产配置。

## 9. 开源版功能边界

本仓库适合作为以下用途：

- 展示电力市场辅助决策系统的产品结构。
- 提供前后端联调框架。
- 提供数据导入、查询和可视化样例。
- 提供网架拓扑和市场数据展示的通用实现思路。

本仓库不包含：

- 真实市场数据。
- 真实数据库。
- API Key、Cookie、UKey、浏览器会话。
- 私有短期价差预测算法。
- 私有中长期价格预测和合约优化算法。
- 未脱敏的网架文件和业务文件。

## 10. 推荐开发流程

```powershell
# 1. 安装后端依赖
cd backend
pip install -r requirements.txt

# 2. 安装前端依赖
cd ..\frontend
npm install

# 3. 启动系统
cd ..
.\start.ps1
```

如果需要单独启动：

```powershell
# 后端
cd backend
python -m uvicorn main:app --host 127.0.0.1 --port 8000 --reload

# 前端
cd frontend
npm run dev
```

## 11. 安全边界

上传 GitHub 前建议执行：

```powershell
git status --short
rg -n "sk-[A-Za-z0-9_-]{20,}|Bearer\s+[A-Za-z0-9._-]{20,}|api[_-]?key|secret|password|cookie|ukey" .
rg --files | rg "(\.env$|config\.local\.json$|\.db$|\.sqlite$|\.xlsx$|\.csv$|\.log$|\.pkl$|node_modules|\.next|downloads|browser-data)"
```

确认没有敏感文件和本地私有数据后再提交。
