# 代码功能介绍

本文档面向 Power Insight Grid 的使用者和二次开发者，介绍项目结构、核心模块和主要功能。

## 1. 项目结构

```text
Power Insight Grid
├─ frontend              # Next.js 前端应用
├─ backend               # FastAPI 后端服务
├─ gd-market-crawler     # 市场数据获取控制服务
├─ deploy                # 部署示例
└─ docs                  # 项目文档
```

系统采用前后端分离结构。前端负责页面交互和图表展示，后端负责数据解析、数据库读写和 API 服务，数据获取控制服务负责管理外部市场数据下载任务。

典型数据流如下：

```text
Excel / CSV / 数据获取结果
        ↓
后端解析与入库
        ↓
SQLite 本地数据库
        ↓
FastAPI 查询接口
        ↓
Next.js 前端页面
```

## 2. 前端应用

目录：

```text
frontend
```

前端应用提供交易辅助决策系统的主要页面，包括：

- 首页和交易日状态。
- 现货模块。
- 基本面数据展示。
- 市场出清与分时电量对比。
- 节点电价查看入口。
- 网架拓扑和阻塞识别。
- 政策文件管理。
- 数据获取和导入管理。

主要技术栈：

- Next.js
- React
- TypeScript
- CSS Modules / CSS

前端页面通过 API 获取后端数据，并在页面中完成日期选择、对比展示、图表联动、状态提示和空数据提示。

## 3. 后端服务

目录：

```text
backend
```

后端服务提供 FastAPI 接口，负责数据读取、解析、入库和查询。

主要能力：

- 管理本地 SQLite 数据库。
- 解析 Excel / CSV 等结构化数据文件。
- 提供交易日上下文接口。
- 提供基本面、市场出清、分时电量和节点电价查询接口。
- 提供网架拓扑节点、线路和阻塞识别结果查询接口。
- 提供数据导入、同步状态和数据质量检查接口。

## 4. 数据库与数据导入

项目默认使用本地 SQLite 数据库。数据库文件通常在本地初始化或导入演示数据后生成。

推荐数据导入流程：

1. 准备脱敏后的 Excel / CSV 数据文件。
2. 使用导入脚本或导入接口写入 SQLite。
3. 通过后端 API 查询数据。
4. 在前端页面按交易日、数据类型和市场口径展示。

真实业务数据、数据库文件、上传文件和生成结果不应提交到公开仓库。

## 5. 现货市场模块

现货模块用于展示电力市场交易日相关数据，支持基本面、市场出清、分时电量和节点电价等信息。

主要功能：

- 按交易日查看数据状态。
- 展示日前和实时口径的数据。
- 支持不同日期之间的对比。
- 支持分电源类型查看出清电量和开机台数。
- 支持图表浮窗、日期标签和空数据提示。

## 6. 基本面数据模块

基本面数据模块用于展示负荷、电源出力、新能源、备用、联络线和安全裕度等信息。

主要功能：

- 多指标联动展示。
- 日前与实时数据对比。
- 运行日与对比日对比。
- 分时点查看。
- 开停机约束与机组出力能力联动展示。

## 7. 市场出清与分时电量模块

该模块用于分析日前与实时市场结果之间的差异。

主要功能：

- 日前与实时口径同屏对比。
- 运行日与对比日对比。
- 出清价格曲线展示。
- 分类型出清电量展示。
- 分类型开机台数展示。
- 支持燃煤、燃气、风电、太阳能、核电、储能和抽蓄等类型。

## 8. 网架拓扑模块

网架拓扑模块用于展示线路、节点和价格分化关系，并辅助识别潜在阻塞线路。

主要功能：

- 加载网架节点和线路关系。
- 根据节点价格计算线路两端价差。
- 按时点查看阻塞识别结果。
- 支持全天汇总和分时查看。
- 支持拓扑图缩放、线路悬停和节点信息查看。
- 支持结合断面、披露阻塞信息和基本面数据进行原因研判。

公开版本保留通用分析框架和演示逻辑，不包含真实网架数据或未脱敏业务文件。

## 9. 政策文件模块

政策文件模块用于管理电力市场政策、交易规则、通知和实施细则。

主要功能：

- 政策文件上传和管理。
- 政策文本摘要展示。
- 结构化字段展示。
- 可扩展接入本地模型或外部模型进行政策解读。

## 10. 数据获取工具

目录：

```text
gd-market-crawler
```

数据获取控制服务用于管理外部市场数据下载任务。

主要功能：

- 按日期范围创建获取任务。
- 检查缺失数据。
- 按缺失清单补充获取。
- 覆盖已有文件。
- 校验下载文件是否为空或结构异常。
- 同步有效文件进入数据库。
- 查看运行日志和中断执行。

该服务需要使用者在本地配置登录方式、下载目录和数据源访问条件。公开仓库不包含真实登录会话、Cookie、UKey 信息或浏览器缓存。

## 11. 导入管理模块

导入管理模块用于将本地文件或演示数据同步到系统数据库。

主要功能：

- 查看可导入的数据项。
- 检查文件是否已入库。
- 导入指定日期范围的数据。
- 查看导入状态和错误提示。
- 为前端页面提供统一的数据可用性状态。

## 12. 部署示例

目录：

```text
deploy
```

部署目录可用于放置本地启动脚本、服务配置示例、反向代理示例或容器化配置。

生产环境部署时，应根据实际网络、权限、数据库和数据源情况重新配置。

## 13. 功能边界

本项目适合用于：

- 学习电力市场辅助决策系统的前后端结构。
- 搭建本地市场数据展示和分析工具。
- 扩展数据导入、查询和可视化能力。
- 验证网架拓扑和市场数据联动的通用实现方式。

公开仓库不包含：

- 真实市场数据。
- 真实数据库。
- API Key、Cookie、UKey 或浏览器会话。
- 私有预测算法和模型产物。
- 未脱敏的网架文件和业务文件。
